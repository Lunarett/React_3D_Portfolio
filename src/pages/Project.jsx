import React from 'react'
import { SectionWrapper } from '../hoc'

const ProjectCard = () => {

}

const Project = () => {
    return (
        <div>Project</div>
    );
}

export default SectionWrapper(Project, 'project')